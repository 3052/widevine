# blog

~~~
gorename -from '".".Container.Key' -to _Key
gorename -from '".".Container.Type' -to _Type
gorename -from '".".Container.Label' -to _Label
gorename -from '".".Container.String' -to _String
gorename -from '".".Containers.Content' -to _Content
gorename -from '".".Poster.Request_URL' -to _Request_URL
gorename -from '".".Poster.Request_Header' -to _Request_Header
gorename -from '".".Poster.Request_Body' -to _Request_Body
gorename -from '".".Poster.Response_Body' -to _Response_Body
gorename -from '".".no_operation.Read' -to _Read
gorename -from '".".Container' -to _Container
gorename -from '".".Module' -to _Module
gorename -from '".".Containers' -to _Containers
gorename -from '".".New_Module' -to _New_Module
gorename -from '".".Poster' -to _Poster
~~~
